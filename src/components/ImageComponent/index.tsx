type ImageComponentProps = {
  title: string,
  paragraph: string
}

const ImageComponent = ({ title, paragraph }: ImageComponentProps) => {
  return (
    <div></div>
  )
}

export default ImageComponent;